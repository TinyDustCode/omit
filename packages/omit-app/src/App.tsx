import {useState} from 'react'import {Button, OmitStyles, OmitProvider} from "omit-ui";function App() {    const [mode, setMode] = useState<'light' | 'dark'>('light')    return (        <OmitProvider theme={{mode}}>            <OmitStyles/>            <div style={{                height: '100vh',                position: 'absolute',                top: 0,                left: 0,                width: '100vw',                boxSizing:"border-box",                padding:100,                background: mode === 'light' ? 'white' : 'black'            }}>                <div>                    <button onClick={() => setMode('dark')}>dark</button>                    <button onClick={() => setMode('light')}>light</button>                </div>                <div>                    <p>色彩</p>                    <Button>默认</Button>                    <Button theme='primary'>primary</Button>                    <Button theme='success'>success</Button>                    <Button theme='warning'>warning</Button>                    <Button theme='danger'>danger</Button>                </div>                <div>                    <p>大小</p>                    <Button theme='success' >默认</Button>                    <Button theme='success' size='small'>small</Button>                    <Button theme='success' size='medium'>medium</Button>                    <Button theme='success' size='large'>danger</Button>                </div>                <div>                    <p>形状</p>                    <Button theme='warning'>默认</Button>                    <Button theme='warning' shape='rectangle'>rectangle</Button>                    <Button theme='warning' shape='round'>round</Button>                    <Button theme='warning' shape='circle'>T</Button>                </div>            </div>        </OmitProvider>    )}export default App